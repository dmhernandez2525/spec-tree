import { describe, it, expect } from 'vitest';
import { AspectRatio } from './aspect-ratio';

describe('AspectRatio exports', () => {
  it('exports AspectRatio component', () => {
    expect(AspectRatio).toBeDefined();
  });
});
